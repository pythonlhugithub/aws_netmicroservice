apiVersion: v1
kind: Service
metadata:
  name: {{ template "agslp.fullname" . }}
  labels:
    app: {{ template "agslp.name" . }}
    chart: {{ template "agslp.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "agslp.name" . }}
    release: {{ .Release.Name }}
